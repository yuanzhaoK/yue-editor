/**
 * Daphne Editor Engine - Root Constants
 * 根元素相关的常量定义
 */

/** 根元素标识 */
export const ROOT = 'daphne';

/** 根元素属性键 */
export const ROOT_KEY = 'data-daphne-root';

/** 根元素选择器 */
export const ROOT_SELECTOR = `[${ROOT_KEY}=${ROOT}]`;
